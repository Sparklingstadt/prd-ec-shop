export function createOrder({
  userId,
  paymentStatus,
  shippingStatus,
  shippingPrice,
  totalPrice
}: {
  userId: number,
  paymentStatus: string,
  shippingStatus: string,
  shippingPrice: number,
  totalPrice: number
}) {
  // prismaのコード
  return
}